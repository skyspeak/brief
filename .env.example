# ── delivery (Resend) ────────────────────────────────────────────────
RESEND_API_KEY=re_xxx
RESEND_WEBHOOK_SECRET=whsec_xxx          # from the Resend webhook you create
DIGEST_TO=you@you.com                     # default recipient (personas can override)
DIGEST_FROM=digest@yourdomain.com         # a domain you've verified in Resend
DIGEST_TITLE=THE BRIEF                     # masthead wordmark (optional)

# ── storage (Turso / libSQL) ─────────────────────────────────────────
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=xxx

# ── model (switchable; used for summaries, digests, AND the ask page) ─
LLM_PROVIDER=claude                       # claude | gemini | openrouter
LLM_MODEL=                                # optional override; REQUIRED for openrouter
ANTHROPIC_API_KEY=sk-ant-xxx              # if provider=claude
GEMINI_API_KEY=                           # if provider=gemini
OPENROUTER_API_KEY=                       # if provider=openrouter

# ── scheduling / access ──────────────────────────────────────────────
CRON_SECRET=generate-a-long-random-string # guards /api/digest, /api/preview,
                                          # and is the access key for the ask page (/)
OUTPUT_FORMAT=pdf                         # pdf | html
DIGEST_WINDOW_DAYS=7
